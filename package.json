{
  "name": "music-piano",
  "version": "1.0.0",
  "description": "音乐弹奏 APP - 安卓版",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed - pure HTML app'",
    "sync": "npx cap sync",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
